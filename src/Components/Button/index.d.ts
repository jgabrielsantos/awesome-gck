import React from 'react';
import { ButtonPropsType } from './types';
export declare const Button: ({ className, children, size, theme, loading, disable, role }: ButtonPropsType) => React.JSX.Element;
