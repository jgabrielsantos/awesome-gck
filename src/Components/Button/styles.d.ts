import { SizeType } from '../../../types';
type ButtonStyleProps = {
    readonly size: SizeType;
    readonly role: string;
};
export declare const ButtonStyled: import("styled-components").StyledComponent<"button", any, ButtonStyleProps, never>;
export {};
